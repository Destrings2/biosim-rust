[package]
name = "biosim4-sensors"
version = "0.1.0"
edition = "2021"
description = "Built-in sensor implementations for biosim4-core"

[dependencies]
biosim4-core = { path = "../biosim4-core" }

[lints]
workspace = true
