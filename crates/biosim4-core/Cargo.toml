[package]
name = "biosim4-core"
version = "0.1.0"
edition = "2021"

[features]
default = []
parallel = ["dep:rayon"]

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
rand = { workspace = true }
rayon = { version = "1.8", optional = true }

[lints]
workspace = true
